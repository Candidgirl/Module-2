# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```python 
i=int(input())
j=int(input())

f = lambda a, b: a+b

print(f(i, j))
```

## Output
<img width="679" height="231" alt="image" src="https://github.com/user-attachments/assets/0f6dfe4e-5964-46e1-813e-a28d6c08eb98" />


## Result
Thus the python program for to defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum has verified successfully

